[package]
name = "gamekit"
version.workspace = true
edition = "2024"
license = "Apache-2.0"

# The games' shared save-state mechanism: serde_json snapshots in day-part-prefs, written on
# game exit (scope cleanup) and on app backgrounding (lifecycle phases where delivered).

[dependencies]
day-core = { git = "https://github.com/daybrite/day.git" }
day-reactive = { git = "https://github.com/daybrite/day.git" }
day-spec = { git = "https://github.com/daybrite/day.git" }
day-part-prefs = { git = "https://github.com/daybrite/day.git" }
# The shared game chrome (`chrome`): composed from Day's core pieces, localized through
# Fluent, with sounds and haptics behind each game's Sounds and Vibrations settings.
day-pieces = { git = "https://github.com/daybrite/day.git" }
day-fluent = { git = "https://github.com/daybrite/day.git" }
day-part-haptics = { git = "https://github.com/daybrite/day.git" }
day-part-sound = { git = "https://github.com/daybrite/day.git" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

# The web build has no `SystemTime`; `seed()` draws browser entropy through the getrandom
# bridge day-dom already answers (docs/web.md).
[target.'cfg(target_arch = "wasm32")'.dependencies]
getrandom = { version = "0.4", default-features = false }

[build-dependencies]
day-build = { git = "https://github.com/daybrite/day.git" }
